{
  "name": "@rigorrun/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply rigorrun --local",
    "db:remote": "wrangler d1 migrations apply rigorrun --remote"
  },
  "dependencies": {
    "@rigorrun/core": "workspace:*",
    "hono": "^4.10.4",
    "zod": "^4.1.12"
  }
}
